# 电站价格整理器项目上下文

## 工作场景

用户需要维护大量电站的充电价格表。价格来源来自不同 App 和微信小程序，用户通过 PixPin 截图并复制 OCR 文本，再粘贴到本项目的本地网页工具中整理价格。

当前工具文件：

- `电站价格整理器.html`

工具目标是从 PixPin OCR 文本中提取 3 个目标时段的会员价和非会员价，并按固定顺序输出一行可粘贴到表格的数据。

## 输出字段顺序

一键复制顺序固定为：

1. 晚谷非会员价
2. 晚谷会员价
3. 中谷非会员价
4. 中谷会员价
5. 平非会员价
6. 平会员价

## 目标时段

- 晚谷：`00:00-06:00`
- 平：`06:00-12:00`
- 中谷：`12:00-14:00`

## 当前取价规则

### 会员价与非会员价

- 同一目标时段只识别到一个有效总价时，会员价和非会员价登记为同价。
- 同一目标时段识别到两个有效总价时，较低价登记为会员价，较高价登记为非会员价。
- 同一目标时段识别到两个以上有效总价时，只取最低的两个价格，最低价为会员价，第二低价为非会员价。
- 不同平台会员名称不作为最终判断依据，最终以价格高低为准。

### 有效总价识别

- 页面直接展示总价时，以展示出的充电单价、当前价、电站价、会员价、VIP 价、闪联价等总价候选为准。
- 页面直接展示总价时，也要识别华自价、YKC价等平台专有价格标签。
- 若页面展示 `电费 + 服务费 = 收费金额/当前价`，只取相加后的收费金额或当前价。
- 若页面展示 `电费 + 服务费 - 优惠 = 当前价`，只取扣减优惠后的当前价。
- 电费、服务费、优惠金额、负数优惠金额、立减金额、补贴金额不直接参与会员/非会员比价。
- 如果相邻数字存在公式关系，应优先识别公式关系中的总价，而不是把分价拿去排序。

### 实际时段匹配

- 实际时段与目标时段重叠时，即纳入该目标时段比价。例如 `00:00-05:59` 可纳入晚谷。
- 实际时段被拆分时，例如 `00:00-02:00`、`02:00-06:00`，分别纳入晚谷候选，并取其中最低会员价所在价格组。
- 实际时段跨越多个目标时段时，例如 `02:30-24:00`，同时纳入被覆盖的晚谷、平、中谷等目标时段。
- 若两个拆分时段共同覆盖目标时段且价格一致，登记结果应保持一致。

## 重要解析策略

工具不能只按“时段后面的数字”取值，因为 PixPin OCR 文本中价格组可能出现在对应时段前面。

当前应遵循两层解析：

1. 先识别所有时段块。
2. 再识别所有价格组。
3. 将价格组归属到前后最合理的时段。
4. 最后做“目标时段 -> 实际时段 -> 价格组”的复核。

价格组示例：

```text
闪联价
0.49
0.33
0.16
VIP价
0.47
0.33
0.14
```

应识别为两个价格组：

- 闪联价：总价 `0.49`
- VIP价：总价 `0.47`

如果价格组紧挨着下一个时段、并且视觉/业务上属于下一个时段，也需要归属到后面的时段。

最新归属策略：若同一页面反复出现固定价格标签组合，例如 `华自价 + VIP价` 或 `闪联价 + VIP价`，应先推断这一轮价格标签模板，再按模板给每个时段补齐价格组。某个价格组即使出现在时段前面，只要它是该时段缺失的模板项，也应归属到该时段，而不是只按距离最近归属。

示例：

```text
00:00-06:00
华自价
0.32
0.31
0.63
VIP价
0.32
0.26
0.58
当前时间
06:00-12:00
华自价
0.66
0.31
0.97
VIP价
0.66
0.26
0.92
12:00-14:00
华自价
0.32
0.35
0.67
VIP价
0.32
0.31
0.63
```

期望输出：

```text
0.6300	0.5800	0.6700	0.6300	0.9700	0.9200
```

## 复核环节

页面中已有“价格组复核”区域，位置在结果卡片下方、复制按钮上方。

复核区应显示：

- 目标时段
- 命中的实际时段
- 对应价格标签
- 识别到的价格组

若某个目标时段只识别到一个有效总价，页面应醒目提示：已按会员/非会员同价处理，需要人工复核。

## 已处理过的关键样例

### 价格组出现在时段前后混排

```text
00:00-06:00
闪联价
0.49
0.33
0.16
VIP价
0.47
0.33
0.14
闪联价
0.82
0.66
0.16
06:00-12:00
VIP价
0.80
0.66
0.14
闪联价
0.49
0.33
0.16
12:00-14:00
VIP价
0.47
0.33
0.14
```

期望输出：

```text
0.4900	0.4700	0.4900	0.4700	0.8200	0.8000
```

### 跨多个目标时段

```text
电站价
YKC价
会员价
02:30~24:00
当前时段
0.8800
0.8800
0.8695
电费
0.6190
0.6190
0.6190
服务费
0.2610
0.2610
0.2505最高优惠5元
00:00~02:30
0.8800
0.8800
0.8695
电费
0.6190
0.6190
0.6190
服务费
0.2610
0.2610
0.2505最高优惠5元
```

期望输出：

```text
0.8800	0.8695	0.8800	0.8695	0.8800	0.8695
```

### 电费 + 服务费 = 当前价

```text
06:00~12:00
0.6565
0.1500
0.8065元/度
12:00~14:00
0.3208
0.2000
0.5208元/度
00:00~06:00
0.3208
0.2000
0.5208元/度
```

期望输出：

```text
0.5208	0.5208	0.5208	0.5208	0.8065	0.8065
```

## 界面要求

- “当前取价规则”默认折叠。
- 规则标题右侧显示状态：
  - 折叠时：`已折叠，点击展开`
  - 展开时：`已展开，点击折叠`
- `例1 / 例2 / 例3 / 清空 / 整理价格` 按钮放在 PixPin 粘贴区下方，方便粘贴后点击。
- 异常提示要醒目。
- 页面应保持本地单文件可用，不依赖联网、不依赖服务器。

## 维护注意

- 后续新增规则时，优先补充解析逻辑和规则说明，不要只改最终输出。
- 每次调整后，必须用历史所有关键样例做回归测试，不能只测当前失败样例。当前回归集至少包括：黑钻会员价/挂牌价、会员专享/优惠价/电站价、电站价单价、会员专享三列+电站价三列、多总价候选+电费服务费干扰、全天最低前置价格、电费+服务费=当前价、02:30-24:00 跨时段、闪联价前后混排、华自价/VIP价常规。
- 不要把电费、服务费、优惠金额误当成会员价。
- 不要只按文本顺序或价格标签判断会员/非会员，最终仍以同一时段有效总价的价格高低决定。
