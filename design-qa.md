# 异常提示设计 QA

- 最终结果：passed
- 设计参照：`C:\Users\15967\.codex\generated_images\019f56fb-1b2b-7613-b240-0ebfea15d00d\exec-21533c30-cf0c-4a33-b88f-ee404f2da2cf.png`
- 长沙复审截图：`D:\AI-price-effect\price-effect\.audit\2026-07-14-region-warning\01-changsha-warning.png`
- 江西复审截图：`D:\AI-price-effect\price-effect\.audit\2026-07-14-region-warning\03-jiangxi-warning-direct.png`
- 对照视口：1672 × 941，桌面端
- 对照状态：本次复制内容完整，但另有 1 个未选时段缺失价格

## 2026-07-14 地区一致性复审

长沙和江西均使用同一套红色异常提示：标题为“另有 1 个时段待核对”，面板背景为 `rgb(254, 243, 242)`，独立感叹号为 `rgb(217, 45, 32)`，语义状态统一为 `role="alert"`。两地均不再生成 `info-only` 降级样式。

第一张江西 iframe 截图出现浏览器合成黑块，已判定为无效证据；随后改用 Chrome 直接页面捕获重新验证，最终截图完整、状态正确。

## 对照结论

已在同一轮视觉输入中并排检查设计参照与实现截图。实现保留了现有产品的信息架构和品牌橙色主操作，同时落实了设计稿的核心意图：结果区顶部出现高对比度红色异常面板，逐项列出时段、时间、原因和严重级别；“开始处理”成为异常状态下最明确的主操作；结果卡片、核心价格输入和复制按钮同步呈现问题状态。

完整视图足以覆盖本次改动的全部关键区域，异常面板位于画面上半部且在原始分辨率下文字清晰，因此未另做局部裁切。

## 检查项

- 字体与层级：标题、摘要、问题原因和状态标签层级清楚；长原因可换行，无截断。
- 布局与间距：异常面板与复制设置、结果卡片保持稳定分组；桌面端无重叠或溢出。
- 颜色与语义：缺失项使用红色，需人工核对使用琥珀色；两者不再只依赖文字区分。
- 图标：面板主提示改为无圆形底的独立红色感叹号；列表内状态图标继续沿用现有线性图标体系。
- 文案：采用“发现 N 个待核对项—逐项确认—开始处理—确认仍要复制”的连续行动语言。
- 交互：点击“开始处理”后进入问题模式、展开明细并将按钮切换为“查看全部”；交互校验未捕获运行时错误。
- 正常态：异常面板隐藏，复制按钮恢复品牌橙色主操作；`actual-fast` 样例复制顺序保持不变。
- 响应式：已检查 1200 × 1000 与 768 × 1152；平板端沿用既有横向结果浏览方式，移动端异常摘要和问题列表纵向排列，无控件重叠。
- 可访问性：只要存在待核对项，异常面板统一使用 `role="alert"` 与 `aria-live="assertive"`；按钮保留原生键盘操作能力；红色与文字、图标、标签共同传达状态。

## 回归结果

7 个内置与历史样例全部通过：`actual-fast`、`actual-diamond`、`actual-huazi`、`actual-three`、`history-mixed`、`history-formula`、`history-cross`。复制顺序仍为：晚谷非会员、晚谷会员、中谷非会员、中谷会员、平非会员、平会员。

## 迭代记录

1. 将原有单行提示改为逐项问题列表，并加入红色感叹号、严重级别标签和“开始处理”按钮。
2. 把问题语义同步到复制预览、核心价格输入与风险复制按钮，避免只有顶部提示明显、下方仍像正常结果。
3. 完成桌面、平板和移动端检查；浏览器回归截图中 7 个样例均通过。
4. 取消“未选时段问题”的橙色/灰色降级样式，统一长沙和江西的红色提醒，并移除主提示感叹号的圆形底。

## 未解决问题

无 P0、P1 或 P2 问题。
