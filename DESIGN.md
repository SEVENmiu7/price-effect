# 电站价格整理器设计规范

## 1. 产品目标

这是一个连续处理大量电价文本的高频办公工具。界面必须服务于以下循环：

粘贴文本 → 自动整理 → 查看异常 → 复制结果 → 清空 → 粘贴下一份。

衡量界面质量的首要标准是操作速度和判断效率，不是功能展示量。正常状态应安静、直接；只有异常和主要操作可以主动吸引注意力。

## 2. 设计原则

- 主页面只呈现当前任务必需的信息，低频能力通过浮层、菜单或折叠区域进入。
- 每个区域只设一个视觉主动作。复制结果区突出“复制结果”，输入区突出“整理价格”。
- 使用对齐、间距和字重建立层级，少用卡片、阴影、图标和说明文字。
- 高频按钮保持大尺寸、固定位置和明确文字，缩短鼠标移动距离。
- 默认展示结果，不默认展示识别过程。异常只在真实存在时出现。
- 保留用户已经形成的工作习惯，不为视觉新鲜感改变关键操作位置。

## 3. 视觉基调

整体气质为克制、温暖、可靠的专业工具。沿用现有暖橙色和米白色，不照搬 Superhuman、Notion 或其他品牌外观。

### 颜色

```css
--color-canvas: #fffdfb;
--color-surface: #ffffff;
--color-surface-soft: #fff8f3;
--color-primary: #df531c;
--color-primary-hover: #c94614;
--color-primary-soft: #fff0e7;
--color-text: #25211f;
--color-text-muted: #746d68;
--color-border: #eadbd2;
--color-border-strong: #d9c5b9;
--color-warning: #b94d16;
--color-warning-bg: #fff3e8;
--color-danger: #c83b32;
--color-success: #21845a;
```

- 橙色只用于主要动作、焦点状态和需要处理的异常。
- 正文使用暖黑色，不使用纯黑。
- 正常状态不使用彩色状态块；成功信息应短暂且克制。
- 背景层级最多三层：页面、主要表面、轻提示表面。

## 4. 字体与信息层级

使用本地系统中文字体栈，保持离线可用：

```css
font-family: "Microsoft YaHei UI", "Microsoft YaHei", "PingFang SC", sans-serif;
```

| 用途 | 字号 | 字重 | 行高 |
|---|---:|---:|---:|
| 页面标题 | 22px | 700 | 1.3 |
| 区域标题 | 18px | 700 | 1.35 |
| 小节标题 | 15px | 700 | 1.4 |
| 正文与控件 | 14px | 400–600 | 1.5 |
| 辅助信息 | 12px | 400 | 1.45 |
| 结果数字 | 16px | 600 | 1.4 |

- 标题直接描述内容，不使用营销式副标题。
- 帮助文字能删除就删除，能缩成一句就不写两句。
- 价格统一显示四位小数，数字列右对齐或使用等宽数字特性。

## 5. 间距、形状与层次

### 间距

以 4px 为基础单位，常用间距为 8、12、16、20、24、32px。

- 页面左右留白：桌面端 32–40px。
- 两栏间距：20–24px。
- 面板内边距：20–24px。
- 紧凑表格行高：52–56px。
- 高频主按钮高度：52–56px。
- 顶部紧凑按钮高度：40–44px。

### 圆角

```css
--radius-sm: 6px;
--radius-md: 9px;
--radius-lg: 14px;
```

- 输入框和普通按钮使用 6–9px。
- 主要面板使用 14px。
- 常规按钮不用胶囊形，状态标签才可使用全圆角。

### 阴影与边框

- 默认用 1px 暖灰边框分组。
- 主页面最多使用一层轻阴影，浮层和模态框才使用明显阴影。
- 不做卡片套卡片；表格行使用分隔线，不拆成独立小卡片。

## 6. 页面结构

### 顶部栏

只保留：产品标题、当前地区与月份、当前取价规则、更多设置。

- 地区与月份合并为紧凑状态按钮，例如“长沙 · 2026年06月”。
- 当前取价规则通过轻量浮层查看。
- 常用时段、输出顺序、导出、使用说明和内部样例归入“更多设置”。
- 不显示流程步骤、状态环、装饰图标或长副标题。

### 左栏：粘贴区

- 标题、大输入框、“清空”和“整理价格”两个并排大按钮。
- 粘贴完成后自动整理；普通键盘输入不触发自动整理。
- 清空不确认，只清除本次输入和结果，并立即聚焦输入框。
- 不显示字符统计、示例入口和大段提示。

### 右栏：结果区

内容顺序固定为：

1. 复制结果标题与当前复制顺序；
2. 实际复制内容预览；
3. 大号“复制结果”按钮；
4. 仅在有问题时出现的异常提示；
5. 晚谷、中谷、平的六项可编辑核心价格；
6. 一个“展开详细明细”入口。

复制内容预览必须与剪贴板内容完全一致。修改核心价格或输出顺序后，预览立即同步。

## 7. 组件规范

### 主要按钮

- 暖橙色实底、白色文字、600–700 字重。
- 不依赖图标表达含义。
- 悬停只加深颜色，不位移、不做夸张阴影。
- 复制成功后短暂将文字改为“已复制”，按钮尺寸不得变化。

### 次要按钮

- 白色或透明背景、暖灰边框、深色文字。
- “清空”是高频次要动作，尺寸与“整理价格”一致，但视觉权重更低。

### 输入框

- 默认边框清晰，聚焦时使用橙色焦点环。
- 文本区域允许长内容滚动，不用装饰性背景。
- 价格输入框直接显示四位小数；缺失项显示“待核对”。

### 异常提示

- 无异常时完全隐藏。
- 有异常时使用浅橙背景、深橙文字和单行摘要。
- 提供“只看问题项”；进入问题模式后改为“查看全部”。
- 不使用闪烁、抖动或自动弹窗。

### 浮层与折叠区

- 浮层靠近触发按钮，单次只打开一个。
- 详细明细默认折叠，展开后保留现有编辑与核对能力。
- 更多设置按任务分组，不做管理后台式导航。

## 8. 响应式与无障碍

- ≥ 1024px 使用左右两栏，左栏约占 38%–42%。
- 768–1023px 仍优先双栏；空间不足时减少留白，不压缩按钮点击区域。
- < 768px 改为单栏：粘贴区在前，结果区在后。
- 所有可点击控件有效高度至少 40px，高频按钮至少 52px。
- 键盘焦点必须清晰；浮层支持 Escape 关闭。
- 状态不能只靠颜色表达，异常同时提供文字。

## 9. 动效

- 常规过渡为 120–180ms，使用 ease-out。
- 仅用于浮层出现、按钮反馈和折叠展开。
- 不做页面入场动画、卡片漂浮、循环装饰或影响连续操作的效果。

## 10. 禁止事项

- 不把界面做成 Dashboard、数据大屏或管理后台。
- 不在主页面摊开所有功能。
- 不使用流程条、状态环、大面积识别说明和重复结果概览。
- 不使用紫色渐变、玻璃拟态、厚重阴影或无意义装饰。
- 不把每项价格做成大卡片。
- 不新增 CDN、在线字体、服务器接口或构建依赖。
- 不因视觉重构改动解析规则、价格归属规则或复制顺序语义。

## 11. 完成检查

- 第一眼能看到粘贴区和复制结果。
- 不阅读说明也能完成“粘贴—复制—清空—下一份”。
- 正常状态安静，异常状态清楚。
- 复制结果、核心价格与详细明细的数据保持一致。
- `index.html` 与 `电站价格整理器.html` 始终保持一致。
- 桌面端 1440×1024 下首屏包含完整高频操作链路。
